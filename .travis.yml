sudo: required
services:
  - docker

before_install:
  - docker build -t jordandlaman/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jordandlaman/docker-react npm run test -- --coverage
